# Overview

`tof-bridge-planning-method` is a reduced public-safe baseline for bridge planning.

It is meant to show how prepared building blocks can continue into rough sketches and target candidates without claiming they are already runtime truth.
